# PHP / NGINX Docker test

## usage

add a  hosts file entry

```bash
127.0.0.1 php-docker.local
```

start the continer

```bash
docker-compose up
```

check that the page is [served](http://php-docker.local:8080)
